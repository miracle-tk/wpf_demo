<Window x:Class="MatiralDesign.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MatiralDesign"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="MainWindow" Height="348.5" Width="674" >
    <!--TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}"-->

    <StackPanel>
        <Button x:Name="open" Click="Bbb_Click">open</Button>
        <ScrollViewer>

            <Image   Width="{Binding RelativeSource={RelativeSource Mode=Self}, Path=Source.Width}" Height="{Binding RelativeSource={RelativeSource Mode=Self},Path=Source.Height}" x:Name="srcImage"></Image>

        </ScrollViewer>



    </StackPanel>
</Window>
