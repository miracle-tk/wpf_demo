<Window x:Class="test1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wf ="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms" 
         xmlns:wfi ="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        xmlns:local="clr-namespace:test1"
        mc:Ignorable="d"
        xmlns:controls="clr-namespace:NetSDKCS.Control;assembly=NetSDKCSControl"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <DropShadowEffect x:Key="shadoweffect" Opacity="0.5" Color="Red" BlurRadius="50" Direction="315">

        </DropShadowEffect>
        <Style x:Key="ContentControlStyle1" TargetType="{x:Type ContentControl}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContentControl}">
                        <ContentPresenter/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>
    <Grid>
        <Button Width="200" Height="20" Content="click" Click="Button_Click_1"/>
        <FrameworkElement Height="200" Width="200" IsHitTestVisible="True"/>


    </Grid>
</Window>
