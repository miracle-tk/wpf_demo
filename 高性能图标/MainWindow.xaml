<Window x:Class="高性能图表.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:高性能图表"
        xmlns:impl="clr-namespace:高性能图标.impl"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition />
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition />
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="1">
            <impl:HorizontalAxisLabel />
            <impl:HorizontalAxisTick VerticalAlignment="Bottom " Range="0,100" LineLength="20"/>

        </Grid>
        <Grid Grid.Row="1">
            <impl:VerticalAxisTick Range="0,100" HorizontalAlignment="Right"/>
        </Grid>
        <Grid Grid.Row="1" Grid.Column="2">
            <impl:VerticalAxisTick  />

        </Grid>
    </Grid>
</Window>
